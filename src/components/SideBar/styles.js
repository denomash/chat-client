import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    width: 250,
    display: "flex",
    flexDirection: "column",
  },
  userInfo: {
    fontWeight: 500
  },
  button: {},
});
